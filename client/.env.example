# URL base dell'API backend Laravel
# In Docker: il frontend (porta 3000) comunica con nginx (porta 80)
VITE_API_URL=http://localhost:3000/api

# Target del proxy API (opzionale)
# Default: http://nginx:80 (per Docker)
# Per sviluppo locale senza Docker: http://localhost:8000
# VITE_API_TARGET=http://localhost:8000
